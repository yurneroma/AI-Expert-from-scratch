[project]
name = "learning"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "graphviz>=0.21",
    "pytest>=9.0.3",
]

[dependency-groups]
dev = [
    "torch>=2.13.0",
]
